spring:
  application:
    name: "elfalyze"

app:
  name: ${spring.application.name}
  version:
    major: 0
    minor: 1
    patch: 0
  file-storage:
    type: local
    params:
      root-dir: ./file-storage/dev
